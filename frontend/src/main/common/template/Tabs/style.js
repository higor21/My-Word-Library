import { makeStyles } from "@material-ui/core/styles"

export const style = makeStyles(() => ({
    root: {
        flexGrow: 1,
    },
    content_height: {
        height: "100%",
        minHeight: "80vh",
    }
}))