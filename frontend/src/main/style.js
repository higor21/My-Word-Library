import { makeStyles } from "@material-ui/core/styles"

export const style = makeStyles(() => ({
    root: {
        backgroundColor: "#faf5ff",
        height: "100vh",
        paddingTop: "5vh",
    },
}))